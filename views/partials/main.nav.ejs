<!--NavBar-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Assignment Tracker</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <% if(title=="Home") { %>
              <a class="nav-link active" href="/home">
            <% } else {%>
              <a class="nav-link" href="/home">
            <% } %>  
            <i class="fa-solid fa-house-chimney-user"></i> Home</a>
          </li>
          <li class="nav-item">
            
          <li class="nav-item">
            <% if(title=="Assignments") { %>
              <a class="nav-link active" href="/assignmentslist">
            <% } else {%>
              <a class="nav-link" href="/assignmentslist">
            <% } %>  
            <i class="fa-solid fa-assignment"></i> assignments</a>
          </li>
          <li class="nav-item"></li>
          
        </ul>
      </div>
    </div>
  </nav>
